CREATE TABLE IF NOT EXISTS person
(
    person_id INT
(
    4
) UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    id VARCHAR
(
    255
),
    firstName VARCHAR
(
    255
) NOT NULL,
    lastName VARCHAR
(
    255
) NOT NULL,
    birthDate DATE
    ) engine=InnoDB;
